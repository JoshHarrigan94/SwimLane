# Optional Supabase sync

Supabase remains optional and disabled. A future adapter must implement `SwimFeedbackRepository`, require authentication, use user-owned rows with RLS, and queue safe offline writes. Service keys never belong in the browser.
