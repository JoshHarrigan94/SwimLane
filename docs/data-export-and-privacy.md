# Feedback data and privacy

Feedback is local by default and may contain notes, session context, and journey duration. It is never public in this prototype. The repository supports export and reset boundaries; do not include executable or HTML content in imports. Optional Supabase sync is intentionally not enabled until authenticated row-level security is configured.
