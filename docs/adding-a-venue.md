# Adding a venue

Create a `TimetableSourceAdapter` with a descriptive user agent and bounded fetch policy, add a facility JSON file containing only verified static facts, add realistic fixtures, and implement source-specific extraction rules without weakening the generic validation policy. Configure a repository endpoint only after the venue’s published JSON schema is validated.
